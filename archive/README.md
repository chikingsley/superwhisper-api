# Archive

Historical code that is useful for reference but no longer part of the main package.

- `gui-orchestration/`: old Superwhisper GUI controller, worker users, launchd watchdog, and recycle scripts.
- `direct-eleven/`: original standalone ElevenLabs scripts before the maintained audio path moved into `superwhisper_api.audio`.
